Notes:

SendGrid's website doesn't like FireFox--but works fine on Chrome

Ideas:

> Wikipedia "In the news" Update Emailer
- Send an email for substantial updates to the "In the news" section of the english wikipedia home page

https://en.wikipedia.org/wiki/Template:In_the_news


import json
import requests
# Choose language
language_code = 'en' # English

url = base_url + language_code + '/featured/' + date
response = requests.get(url, headers=headers)

# Get a list of headlines
headlines = []

response = json.loads(response.text)
for story in response['news']:
  headline = story['story']
  # Replace relative URLs with absolute URLs
  headline = headline.replace('"./', '"https://' + language_code + '.wikipedia.org/wiki/')
  headlines.append(headline)





